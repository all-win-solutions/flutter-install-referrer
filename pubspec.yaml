name: install_referrer
description: Detect from where your application was installed (Google Play, App Store, TestFlight…)
version: 1.2.1
homepage: https://github.com/g123k/flutter_plugin_install_referrer

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  pigeon: ^22.6.2

flutter:
  plugin:
    platforms:
      android:
        package: fr.g123k.install_referrer
        pluginClass: InstallReferrerPlugin
      ios:
        pluginClass: InstallReferrerPlugin
